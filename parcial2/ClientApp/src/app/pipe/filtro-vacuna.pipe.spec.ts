import { FiltroVacunaPipe } from './filtro-vacuna.pipe';

describe('FiltroVacunaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroVacunaPipe();
    expect(pipe).toBeTruthy();
  });
});
